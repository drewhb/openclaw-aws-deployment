{
  "commands": [
    "sudo -H -u ubuntu XDG_RUNTIME_DIR=/run/user/1000 bash -lc 'export HOME=/home/ubuntu; export NVM_DIR=\"$HOME/.nvm\"; [ -s \"$NVM_DIR/nvm.sh\" ] && . \"$NVM_DIR/nvm.sh\"; openclaw pairing --help 2>&1 || true'"
  ]
}
